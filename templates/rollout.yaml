apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: {{ include "deployedfrontend.fullname" . }}
  labels:
    {{- include "deployedfrontend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "deployedfrontend.selectorLabels" . | nindent 6 }}
  strategy:
    canary:
      # 🧠 Canary steps: gradually shift traffic and pause for observation
      steps:
        - setWeight: 20
        - pause: { duration: 30 }     # pause 30s before next step
        - setWeight: 50
        - pause: { duration: 60 }     # pause 1min before full rollout

      # ✅ Auto rollback if pods fail to become healthy
      progressDeadlineSeconds: 120       # Wait up to 2 min for pods to become healthy
      progressDeadlineAbort: true        # Abort and rollback automatically on failure

      # (Optional) define services if you want separate traffic routing
      # stableService: deployedfrontend-stable
      # canaryService: deployedfrontend-canary

  template:
    metadata:
      labels:
        {{- include "deployedfrontend.labels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
